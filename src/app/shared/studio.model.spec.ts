/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {Studio} from './studio.model';

describe('Studio', () => {
  it('should create an instance', () => {
    expect(new Studio()).toBeTruthy();
  });
});
